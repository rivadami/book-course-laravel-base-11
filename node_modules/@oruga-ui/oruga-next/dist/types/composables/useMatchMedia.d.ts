/**
 * Checks if the match media is mobile.
 * @param mobileBreakpoint px breakpoint
 */
export declare function useMatchMedia(mobileBreakpoint?: string): {
    isMobile: import('vue').Ref<boolean>;
};
